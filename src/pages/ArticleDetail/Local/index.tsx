import React from 'react'
import {
    View,
    Text,
    StyleSheet,
    ScrollView
} from 'react-native'

const Local = () => {
    return (
        <ScrollView></ScrollView>
    )
}

export default Local